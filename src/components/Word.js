import { Box } from "@mui/system";
import React from "react";

function Word() {
  return (
    <Box sx={{ display: "flex", justifyContent: "center" }}>
      this is the word component
    </Box>
  );
}

export default Word;
